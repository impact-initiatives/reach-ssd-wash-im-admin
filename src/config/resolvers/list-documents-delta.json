{
  "version": "2017-02-28",
  "operation": "Scan",
  "filter": {
    "expression": "#updatedAt > :updatedAt",
    "expressionNames": {
      "#updatedAt": "updatedAt"
    },
    "expressionValues": {
      ":updatedAt": { "N": "${ctx.args.lastSync}" }
    }
  }
}

// $util.toJson($ctx.result.items)
